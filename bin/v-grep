#!/bin/bash
# info: calling vesta_grep PHP function
# options: PARAMETERS
#
# The function is calling Vesta PHP replacement for GNU 'grep' command (but without regular expression)

#----------------------------------------------------------#
#                       Action                             #
#----------------------------------------------------------#


if [ "$1" == "--stdin" ] && [ -p /dev/stdin ]; then
    STDIN=$(cat -)
    if [ ! -z "$STDIN" ]; then
        shift;
        echo "$STDIN" | php /usr/local/vesta/func/bash-to-php-interpreter.php '--stdin' 'vesta_grep' "$@"
        exit $?
    fi
fi

if [ "$1" == "--stdin" ]; then
    shift;
    php /usr/local/vesta/func/bash-to-php-interpreter.php '--stdin' 'vesta_grep' "$@"
else
    php /usr/local/vesta/func/bash-to-php-interpreter.php 'vesta_grep' "$@"
fi
exit $?
